# Collection-linter
Scene contract checker. Reads a JSON scene dump (or live bpy), assigns roles, and reports readable faults against Unit-canon. This is the inspectable error surface your process actually persists on.
